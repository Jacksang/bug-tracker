{
  "name": "bug-tracker",
  "version": "1.0.0",
  "description": "Cross-project bug submission form — submit bugs to any project, Eva2 fixes them after you check in",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.0",
    "cors": "^2.8.5",
    "multer": "^2.0.0"
  }
}
